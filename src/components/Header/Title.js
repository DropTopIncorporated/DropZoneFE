import React from 'react';
import styles from './Title.css';

const Title = () => {

  return (
    <div className={styles.container}>
      <p className={styles.title}>DROPZONE</p>
    </div>
  );
};

export default Title;
